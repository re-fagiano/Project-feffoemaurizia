# Configurazione ambiente - SVILUPPO LOCALE
# Usa SQLite per sviluppo (cambia in PostgreSQL per produzione)
DATABASE_URL=sqlite:///./ticket_platform.db

SECRET_KEY=dev-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Email SMTP (da configurare)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@example.com

# Redis per Celery (opzionale)
REDIS_URL=redis://localhost:6379/0
